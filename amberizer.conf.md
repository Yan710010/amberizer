关于配置文件使用方法的说明

使用键值对形式（`key=value`），以换行分隔设置条目

不包含等号的行不会被解析，因此可以使用任意方式写注释：

```
# 这是注释
// 这也是注释
直接这样也是注释
其实就算你这样写=也不会有关系
（但是这么简单的一个配置到底为什么会需要注释）
```

键和值的两段空格会被去除，因此下面几种写法等价：

```
"AMBERIZER_HOST=127.0.0.1"
"AMBERIZER_HOST = 127.0.0.1"
"   AMBERIZER_HOST  = 127.0.0.1   "
```

可以使用的设置名称与环境变量名相同，但是忽略大小写（注意值（等号右边的部分）不会忽略大小写）：

```
AMBERIZER_PORT = 3001
amberizer_port = 3001
AmBeRiZeR_PoRt = 3001
```


